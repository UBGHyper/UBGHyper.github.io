<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UBGHyper - Home</title>
    <link rel="stylesheet" href="config/main.css">
    <link rel="stylesheet" href="bindex.css">
    <script src="config/main.js" type="module"></script>
    <link rel="icon" type="image/png" href="content/favicons/tab.png">
    <style>
        /* General styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
            color: #333;
            text-align: center;
        }
        .center {
            text-align: center;
            margin: 20px;
        }
        .Apple-font {
            font-family: 'Apple System', sans-serif;
        }
        .footer {
            margin-top: 20px;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
        }
        .game-container {
            display: flex;
            justify-content: center;
            margin: 20px;
        }
        .game-square {
            width: 100px;
            height: 100px;
            background-color: #ff5722;
            border-radius: 10px;
            cursor: pointer;
            transition: transform 0.3s;
        }
        .game-square:hover {
            transform: scale(1.1);
        }
        .switch-container {
            margin: 20px;
        }
        .toggle-switch {
            display: inline-block;
            position: relative;
            width: 60px;
            height: 34px;
        }
        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }
        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: .4s;
            border-radius: 34px;
        }
        .toggle-slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            border-radius: 50%;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
        }
        input:checked + .toggle-slider {
            background-color: #4CAF50;
        }
        input:checked + .toggle-slider:before {
            transform: translateX(26px);
        }
        button {
            background-color: #4CAF50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            margin: 5px;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #45a049;
        }
        select {
            padding: 10px;
            font-size: 16px;
            margin: 10px;
        }
    </style>
</head>
<body>
    <div class="center">
        <h1 class="Apple-font">UBG Hyper</h1>
        <h2 class="Apple-font">↓ Games ↓</h2>
    </div>
    <div class="center">
        <input type="text" id="searchInput" placeholder="Search games" style="padding: 10px; font-size: 16px;">
    </div>
    <div id="gamesContainer"></div>
    <div class="footer">
        <h1 class="Apple-font">A Website By</h1>
        <h1 class="Apple-font">Fibble</h1>
    </div>
    <div class="game-container">
    </div>
    <div class="switch-container">
        <button onclick="window.location.href='https://soggycat0.github.io';">SoggyCat0</button>
            </label>
        </div>
        <button onclick="window.location.href='https://forms.gle/fEKyBwT683ABdtkz6';">Suggestions</button>
    </div>
    <select id="themeSelect">
        <option value="">Default</option>
        <option value="dark-theme">Dark</option>
        <option value="light-theme">Light</option>
    </select>

    <script>
        let konamiCode = [];

        document.addEventListener("keydown", function(event) {
            const keyMap = {
                "ArrowUp": "Up",
                "ArrowDown": "Down",
                "ArrowLeft": "Left",
                "ArrowRight": "Right",
                "b": "B",
                "B": "B",
                "a": "A",
                "A": "A"
            };
            if (keyMap[event.key]) {
                konamiCode.push(keyMap[event.key]);
            }

            if (konamiCode.join(",") === "Up,Up,Down,Down,Left,Right,Left,Right,B,A") {
                konamiCode = [];
                document.body.style.background = getRandomColor();
            }
        });

        function getRandomColor() {
            const letters = "0123456789ABCDEF";
            let color = "#";
            for (let i = 0; i < 6; i++) {
                color += letters[Math.floor(Math.random() * 16)];
            }
            return color;
        }

        const toggleSwitch = document.getElementById("toggle-switch");
        const favicon = document.querySelector("link[rel='icon']");
        const defaultFavicon = "content/favicons/tab.png";
        const toggledFavicon = "content/favicons/classroom.png";
        const defaultTitle = "UBGHyper - Home";
        const toggledTitle = "Classroom";

        toggleSwitch.addEventListener("change", () => {
            favicon.href = toggleSwitch.checked ? toggledFavicon : defaultFavicon;
            document.title = toggleSwitch.checked ? toggledTitle : defaultTitle;
        });

        document.getElementById('themeSelect').addEventListener('change', function() {
            const theme = this.value;
            document.body.className = theme;
            document.querySelector('.footer').className = `footer ${theme}`;
        });

        function unlockSecretGame() {
            alert('Congratulations! You found a secret game!');
            // Code to unlock a secret game goes here
        }
    </script>
</body>
</html>
